# Kyle — Dashboard & UX Refinement — Design Spec

**Date:** 2026-07-29  
**Status:** Approved

## Changes

1. **Page titles:** `%s | Kyle` (pipe, not em-dash)
2. **Firewall icon:** `ArrowsLeftRight` instead of `Shield`
3. **Sidebar font:** `font-medium` for nav items (was `font-light`)
4. **Dashboard KPIs:** no card backgrounds, colored icons (w-5 h-5), pulsing placeholder bars instead of "-- awaiting data"
5. **Dashboard map:** Leaflet 2D full-width replacing 3D globe
6. **KPI drawer:** click opens right-side panel with sparkline + top contributors + key metrics
7. **Refresh button:** unified `ArrowCounterClockwise` icon
